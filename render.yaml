services:
  - type: web
    name: dnsway-api
    runtime: go
    repo: https://github.com/yoshinaga211/dnsway
    branch: main
    plan: free
    envVars:
      - key: ENV
        value: production
    buildCommand: go build -o api ./cmd/api/
    startCommand: ./api
    healthCheckPath: /health
